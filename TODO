Fix connect timeout of slave node. The most efficent way is a, remove time out, and use some thing as a ping_loop.

Cluster test suite (fix some problem with this).

pool

atom_task

atom_task_restart when crash.


BUGFIX some time node1 don't start ping_pong service.

May be some problem when supvisor restart master node.