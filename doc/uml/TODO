add(fix)) layer for access for mnesia

fix problem with master_task_manager (in some diagrams it not use, but
need)

fix problem with inaccurate call.

add supervisor hierarchy for each node.

add node visor hierarchy

add cluster_date_base scheme

maybe need...

add sequence diagram for rfid

add global_tas

add diagrams for each module API